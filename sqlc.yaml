version: "2"
sql:
  - engine: "postgresql"
    queries: "channels/queries/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "queries"
        out: "channels/queries"
        sql_package: "pgx/v5"
        emit_json_tags: true
